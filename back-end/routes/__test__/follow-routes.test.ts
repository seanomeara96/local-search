import request from "supertest";
import app from "../../app";
it("responds with 201 if follow has been created", async () => {
  // stuff goes here
});

it("reponds with 200 if follow has been destroyed", async () => {
  // stuff goes here
});
